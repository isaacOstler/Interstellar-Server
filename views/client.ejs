<!DOCTYPE html>
<html>
<head>
	<title>Interstellar - Server - Version 0.9.2</title>
	<link rel="stylesheet" type="text/css" href="/resource?path=stylesheet.css">
	<script>if (typeof module === 'object') {window.module = module; module = undefined;}</script>
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
	<script>if (window.module) module = window.module;</script>
</head>
<body>
	<div id="sideBar">
	<span style="background-color:#6290bc; width: 100%;height: 55px;position: absolute;top:0px;left:0px;padding-top: 20px;padding-bottom: 20px;">
	<center><b>
		<span style="font-size: 55px">Interstellar</span>
	</b></center>
	</span>


	<div id="stationsList">
		<div id="station%FIRST OFFICER" class="stationOption">
			<% for(var i = 0; i < stations.length;i++){ %>
				<%= stations[i].stationInfo.name %><br />
			<% } %>
			<% if(stations.length == 0){ %>
				<span style="color:red">COULD NOT FIND STATIONS</span>
			<% } %>
		</div>

	</div>
	</div>

	<div id="bottomBar">Server running on port: <div style="position: absolute;left:140px;top:0px;height: 100%;padding-left: 5px;padding-right: 5px;background-color:green"><%= port %></div></div>

	<h2 style="position: absolute;top:-18px;left:31%;color:white;z-index: 3">Widgets Folder</h2>
	<h2 style="position: absolute;top:42%;left:31%;color:white;z-index: 3">Station Widgets</h2>

	<div id="allWidgetsList">
	<span class="tableSelect">
		<table class="tableView">
			<tr>
			<th>Card Name</th>
			<th>Card Type</th>
			<th>Card version</th>
			<th>Card Author</th>
			<th>Card Description</th>
			</tr>
			<% for(var i=0; i < cards.length; i++) { %>
   			<tr class="<%if ((i % 2) == 0) { %>
				lightStripe tableRow
			<% }else{ %>
				darkStripe tableRow
			<% }if(cards[i].cardInfo.cardType == "coreWidget"){%>
			   	coreWidgetRow
			<% }else if(cards[i].cardInfo.cardType == "card"){ %>
			   cardRow
			<% } %>
			 ">
     		<td><%= cards[i].cardInfo.cardName %></td>
     		<td><%= cards[i].cardInfo.cardType %></td>
     		<td><%= cards[i].cardInfo.version %></td>
     		<td style="font-size: 12px"><%= cards[i].cardInfo.author %></td>
     		<td style="font-size: 12px"><%= cards[i].cardInfo.description %></td>
   			</tr>
			<% } %>
		</table>
	</span>
	</div>
	<div id="addedWidgetsView">
		<table class="tableView">
			<tr>
			<th>Card Name</th>
			<th>Card Type</th>
			<th>Card version</th>
			<th>Card Author</th>
			<th>Card Description</th>
			</tr>
			<% for(var i=0; i < cards.length; i++) { %>
   			<tr class="<%if ((i % 2) == 0) { %>
				lightStripe tableRow
			<% }else{ %>
				darkStripe tableRow
			<% }if(cards[i].cardInfo.cardType == "coreWidget"){%>
			   	coreWidgetRow
			<% }else if(cards[i].cardInfo.cardType == "card"){ %>
			   cardRow
			<% } %>
			 ">
     		<td><%= cards[i].cardInfo.cardName %></td>
     		<td><%= cards[i].cardInfo.cardType %></td>
     		<td><%= cards[i].cardInfo.version %></td>
     		<td style="font-size: 12px"><%= cards[i].cardInfo.author %></td>
     		<td style="font-size: 12px"><%= cards[i].cardInfo.description %></td>
   			</tr>
			<% } %>
		</table>
	</span>
	</div>
	<div style="position: absolute;top:3px;right:10px;font-size: 10px;">
	<span id="tableViewField">Table View</span> | <span id="iconViewField">Icon View</span>
	</div>
	<script src="/guiScript"></script>
</body>
</html>